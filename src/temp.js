import { connect } from 'react-redux';
import f from './registerCmp';
//import { ... } from '../../actions';

/*const mapStateToProps = (state) => ({
  ...
});

const mapDispatchToProps = (dispatch, ownProps) => ({
  ...
});
*/

//export default connect(mapStateToPops, mapDispatchToProps)(Register);
export default f;
